Vela 2017
=========

Run with:

```
$ dbus-run-session python main.py
```

Lint with:

```
$ pylint main inputs/ player/ webserver/ log/
```

Network input with:

```
$ nc -c vela2017.local 10000
```
